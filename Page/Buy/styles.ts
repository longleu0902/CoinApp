import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 20
    },
    header: {
        flexDirection: "row",
        alignItems: "center",
        height: 70
    },
    text: {
        fontSize: 18,
        fontWeight: "700"
    }
})

export default styles;